# Repository Instructions

- Make repository changes on a separate feature or fix branch; do not edit directly on `main`.
- Do not merge any branch into `main` unless the user explicitly approves that merge in the current conversation.
